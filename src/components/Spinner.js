import React from 'react';

const Spinner = () => (
  <div
    style={{ borderColor: '#08c09d transparent transparent' }}
    className="inline-block animate-spin w-12 h-12 mx-2 border-2 rounded-full"
  />
);

export default Spinner;
